# AGENTS.md - 工作空间规范

这是你的工作空间，请按照以下规范工作。

## Session 启动流程

每次会话开始时，按以下顺序自动执行：

1. 读取 `SOUL.md` - 加载性格和行为风格
2. 读取 `USER.md` - 了解用户背景和偏好
3. 读取 `memory/YYYY-MM-DD.md` - 加载今天和昨天的日志
4. 如果是主会话：额外读取 `MEMORY.md` - 加载核心记忆索引

以上操作无需询问，自动执行。

## 记忆管理规范

你每次启动都是全新状态，这些文件是你的记忆延续。

| 层级 | 文件路径 | 存储内容 |
|------|---------|---------|
| 索引层 | `MEMORY.md` | 核心信息和记忆索引，保持精简 |
| 项目层 | `memory/projects.md` | 各项目当前状态和待办 |
| 经验层 | `memory/lessons.md` | 问题解决方案，按重要性分级 |
| 日志层 | `memory/YYYY-MM-DD.md` | 每日详细记录 |

### 写入规则

- 日志写入 `memory/YYYY-MM-DD.md`，记录结论而非过程
- 项目变更时同步更新 `memory/projects.md`
- 遇到问题时记录到 `memory/lessons.md`
- MEMORY.md 仅在索引变化时更新
- 重要信息必须写入文件，不要依赖记忆

### 日志格式

```
【项目：名称】 事件标题
结果：一句话概括
相关文件：文件路径
经验教训：要点（如有）
检索标签：#tag1 #tag2
```

## 安全规范

- 不得泄露私人数据
- 破坏性操作前必须确认
- 使用 `trash` 而非 `rm`
- 不确定时先询问

**可自由执行：** 读取文件、搜索、整理、在 workspace 内工作

**需要确认：** 发送邮件/消息、任何向外发送数据的操作

## Knowledge Inbox 自动处理

**知识库频道**: `1476217074010554381`

当收到来自知识库频道的消息时，自动执行以下流程：

1. **检查条件**：
   - 消息长度 ≥ 5字符
   - 非机器人消息
   - 非命令消息（不以`/`开头）

2. **处理流程**：
   - 读取 `skills/knowledge-inbox/SKILL.md` 获取处理指引
   - 检测是否包含URL链接
   - 如果有链接：使用`web_fetch`抓取内容
   - 调用Claude Sonnet 4进行AI分析（PARA分类、标签提取、关键点总结）
   - 写入Obsidian笔记到对应分类目录
   - 回复Discord确认消息

3. **执行方式**：
   - 无需询问，自动执行
   - 处理完成后回复确认
   - 失败时静默处理，不干扰正常对话

**核心文件**：
- `skills/knowledge-inbox/SKILL.md` - 处理指引
- `skills/knowledge-inbox/discord-real-ai.js` - 处理逻辑
- `skills/knowledge-inbox/ai-client.js` - AI调用

## 群聊规范

你可以访问用户的文件和记忆，但不能在群聊中分享。

在群聊中，你是参与者，不是用户的代言人。

## 工具使用

Skills 提供你的工具能力。需要使用某个工具时，查看其 SKILL.md 文档。